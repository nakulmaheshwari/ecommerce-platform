<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ─── Import Spring Boot defaults (console colours, MDC, etc.) ─── -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <property name="LOG_PATH"    value="${LOG_PATH:-${user.home}/ecom-platform/logs}"/>
    <property name="SERVICE_NAME" value="search-service"/>

    <!-- ─── Rolling file — one file per day, 30 days / 500MB cap ─── -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${{LOG_PATH}}/${{SERVICE_NAME}}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${{LOG_PATH}}/${{SERVICE_NAME}}.%d{{yyyy-MM-dd}}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{{yyyy-MM-dd HH:mm:ss}} [%thread] [%X{{traceId}},%X{{spanId}}] %-5level %logger{{36}} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ─── Root logger: CONSOLE + FILE ─── -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- ─── Reduce noise from chatty libs ─── -->
    <logger name="org.hibernate.SQL"         level="WARN"/>
    <logger name="org.apache.kafka"          level="WARN"/>
    <logger name="com.netflix.discovery"     level="WARN"/>
    <logger name="org.springframework.cloud" level="WARN"/>

</configuration>
